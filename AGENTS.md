# AGENTS.md — for any agent reading this estate

Generated 2026-09-18T17:18:27Z from a walk of zistgah, project-ilm.
Regenerated on every walk; do not hand-edit.

## Where everything is

- `https://zistgah.org/ontology.json` and `https://zistgah.org/ontology.md` — how this estate is described, and what
  is not yet described. Build here.
- `https://zistgah.org/catalog.json` — every repository, with URL, clone, API, raw base, README,
  CONTRACT.md, CONTEXT.md, AGENTS.md, CITATION.cff, MANIFEST.yaml, roadmap, descriptor,
  issues, pages and DOI.
- `https://zistgah.org/index.json` and `https://zistgah.org/.well-known/estate.json` — the raw walk.
- `https://zistgah.org/memory-reconciliation.json` — an AI assistant's beliefs, checked against the forge.
- `https://zistgah.org/unminted.json` — what is live and carries no DOI.
- `https://zistgah.org/contributing.md` — how to take work.
- https://github.com/zistgah/governance — the master contract and context.

## What you may do

Read anything. Clone anything. Build on anything, under each repository's own licence.
Open issues and pull requests. Take a work package and finish it: a package is done when
its stated acceptance command exits 0.

## What you may not do

Push to a default branch. Mint a DOI. Edit `attest/`. Those are the author's acts, taken
under a typed gate on his own machine.

## How to be useful

1. Retrieve before asserting. The live state is in the repository, not in your memory of it.
2. Where a value is unknown, write `not established`.
3. A claim needs a recorded measurement in the same change.
4. One package, one pull request, one proof.
5. Report a refusal. Do not route around it.
6. Work is labelled by capability, never by model name.

## Counts, as walked

85 repositories, 26 with a DOI, 47 serving a
page, 0 archived, 4 empty.
